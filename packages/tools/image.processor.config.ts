/**
 * Shared configuration for image-processor based tools.
 *
 * @module tools/image.processor.config
 */

export { IMAGE_PROCESSOR_TIMEOUT_MS, IMAGE_PROCESSOR_URL } from './image.processor.shared';
